name: Deploy

on:
  workflow_run:
    workflows: ["Test"]
    branches: ["main"]
    types: ["completed"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Fake Deploy
        run: echo "Fake deploy completed."
